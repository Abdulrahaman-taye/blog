# Downloads Folder

Place your downloadable files here.

The `downloadUrl` field in `json/downloads.json` points to files in this folder.

Example:
- `downloads/zakat-calculator.xlsx`
- `downloads/islamic-finance-glossary.pdf`

External URLs are also supported in `downloadUrl`.